<div class="room-wrap">
  {{#rooms}}
    <div data-id='{{name}}' class="individual_room room elevencol"><i class="fa fa-comments-o fa-2x"></i>{{name}}<br>
      <span>{{distance}} away</span><span class="user-count">{{userCount}} connected</span></div>
  {{/rooms}}
</div>

